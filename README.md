# PetSense AI

Scaffolded from the supplied project documentation.

## Structure

- `petsenseai_frontend/`: React + Vite frontend scaffold
- `backend/`: FastAPI backend scaffold
- `ai/`: placeholders for RAG, vision, and symptom modules
- `data/`: local data assets and indexes

## Next steps

1. Install frontend dependencies with `npm install` inside `petsenseai_frontend/`
2. Create a Python virtual environment in `backend/`
3. Implement API logic and connect the frontend API layer
